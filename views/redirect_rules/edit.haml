!= View::make('decoy::shared.form._header', $__data)->render()

%fieldset
	.legend=empty($item) ? 'New' : 'Edit'

	:php
		$help = __('decoy::redirect_rules.form.from_help', ['root' => Request::root()]);
		if (Config::get('decoy::core.allow_regex_in_redirects')) {
			$help .= __('decoy::redirect_rules.form.from_regex_help');
		}
		echo Former::text('from')->blockHelp($help);

	!= Former::text('to')->blockHelp(__('decoy::redirect_rules.form.to_help'))
	!= Former::radios('code')->radios(Bkwld\Library\Laravel\Former::radioArray(Bkwld\Decoy\Models\RedirectRule::$codes))->blockHelp(__('decoy::redirect_rules.form.radio_help'));
	!= Former::text('label')->help(__('decoy::redirect_rules.form.label_help'))

!= View::make('decoy::shared.form._footer', $__data)->render()
